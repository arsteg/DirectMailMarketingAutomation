<Window x:Class="MailMergeUI.Views.BlacklistView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:MailMergeUI.Helpers"
        mc:Ignorable="d"
        Title="BlacklistView" Height="450" Width="800">
    <Window.Resources>
        <converters:DateTimeConverter x:Key="DateTimeConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>          
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnBack" Content=" ← Back" Style="{StaticResource LogoutButton}" Click="btnBack_Click"/>
            </StackPanel>
        </Grid>
        <!-- ==================== SEARCH & ADD ==================== -->
        <TextBlock Grid.Row="1"
                Text="Blacklist Management"
                FontSize="20"
                FontWeight="SemiBold"
                Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1"
                 Orientation="Horizontal"
                 Margin="0,0,0,15">
            <TextBox Text="{Binding SearchAddress, UpdateSourceTrigger=PropertyChanged}"
                  Width="300"                 
                  Margin="0,0,10,0"/>
            <Button Content="Blacklist"
                 Command="{Binding BlacklistCommand}"
                 Padding="12,6"/>
        </StackPanel>

        <!-- ==================== LIST ==================== -->
        <DataGrid Grid.Row="2"
               ItemsSource="{Binding Blacklist}"
               AutoGenerateColumns="False"
               CanUserAddRows="False"
               CanUserDeleteRows="False"
               IsReadOnly="True"
               AlternatingRowBackground="#FFF5F5F5"
               GridLinesVisibility="Horizontal">

            <DataGrid.Columns>
                <!-- Address -->
                <DataGridTextColumn Header="Property Address"
                                 Binding="{Binding Address}"
                                 Width="*"/>

                <!-- Blacklisted At -->
                <DataGridTextColumn Header="Blacklisted At"
                                 Binding="{Binding BlacklistedAt, Converter={StaticResource DateTimeConverter}}"
                                 Width="180"/>

                <!-- Remove button -->
                <DataGridTemplateColumn Header="Actions" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remove"
                                 Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                 CommandParameter="{Binding}"
                                 Background="#FFCCCC"
                                 Foreground="DarkRed"
                                 Padding="6,2"
                                 HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>






