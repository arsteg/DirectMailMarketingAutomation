<Window x:Class="MailMergeUI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F9FF"
        ResizeMode="NoResize"
        WindowStyle="None"
        FontFamily="Segoe UI"
        AllowsTransparency="True">

    <Grid Name="SettingsGrid" MouseDown="SettingsGrid_MouseDown">

        <!-- Close Button -->
        <Button Content="✕" 
                Width="30" Height="30" 
                Background="Transparent"
                Foreground="{DynamicResource TextLight}"
                BorderThickness="0"
                HorizontalAlignment="Right" 
                VerticalAlignment="Top"
                Margin="-2"
                FontSize="12"
                Click="CloseButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="15"
                            Width="30" Height="30">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#10000000"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextDark}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- Main Layout -->
        <Grid Margin="0,25,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Bar (same style as MainWindow) -->
            <Border Grid.Row="0" Background="{DynamicResource HeaderBackgroundLight}" 
                    Padding="16,8,16,8" 
                    BorderBrush="{DynamicResource DividerColor}" 
                    BorderThickness="0,0,0,1">
                <Grid>
                    <TextBlock Text="Application Settings" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="{DynamicResource PrimaryBlue}" 
                               VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnBack" Content="⬅ Back" 
                                Style="{StaticResource LogoutButton}" 
                                Click="btnBack_Click"
                                Margin="0,0,12,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Settings Cards Area -->
            <ScrollViewer Background="{DynamicResource BackgroundLight}" Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <WrapPanel Margin="40" ItemWidth="320" ItemHeight="160" Orientation="Horizontal">

                    <!-- Appearance Card -->
                    <Border CornerRadius="8" Margin="12"
                            Background="{DynamicResource CardBackgroundLight}" 
                            BorderBrush="{DynamicResource DividerColor}" 
                            BorderThickness="1">
                        <StackPanel Margin="12" VerticalAlignment="Center">
                            <TextBlock Text="Appearance" 
                                       FontSize="16" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextDark}" 
                                       Margin="0,0,0,8"/>
                            <TextBlock Text="Switch between Light and Dark mode." 
                                       FontSize="13" Foreground="{DynamicResource TextLight}" 
                                       TextWrapping="Wrap" Margin="0,0,0,12"/>
                            <Button x:Name="btnDarkModeToggle"
                                    Content="🌙 Dark Mode"
                                    Style="{StaticResource DarkModeToggleButton}"
                                    Click="btnDarkModeToggle_Click"/>
                        </StackPanel>
                    </Border>

                </WrapPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
